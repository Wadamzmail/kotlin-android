From 394054814f1394007f4f8f56cbd89f93b59129cf Mon Sep 17 00:00:00 2001
From: Akash Yadav <akashyadav@appdevforall.org>
Date: Wed, 25 Mar 2026 21:11:01 +0530
Subject: [PATCH 12/12] fix: add shim for DebugAttachDetectorArgs

Signed-off-by: Akash Yadav <akashyadav@appdevforall.org>
---
 .../build.gradle.kts                          | 11 +++-
 .../util/DebugAttachDetectorArgs.java         | 57 +++++++++++++++++++
 2 files changed, 65 insertions(+), 3 deletions(-)
 create mode 100644 prepare/ide-plugin-dependencies/android-shims/src/main/java/com/android/com/intellij/util/DebugAttachDetectorArgs.java

diff --git a/prepare/ide-plugin-dependencies/analysis-api-standalone-embeddable-for-ide/build.gradle.kts b/prepare/ide-plugin-dependencies/analysis-api-standalone-embeddable-for-ide/build.gradle.kts
index 9ba4b9ba54db..3c1399b40081 100644
--- a/prepare/ide-plugin-dependencies/analysis-api-standalone-embeddable-for-ide/build.gradle.kts
+++ b/prepare/ide-plugin-dependencies/analysis-api-standalone-embeddable-for-ide/build.gradle.kts
@@ -28,8 +28,8 @@ dependencies {
     embeddableJar(intellijCore())
     embeddableJar(libs.intellij.fastutil)
     embeddableJar(commonDependency("com.fasterxml:aalto-xml"))
-    embeddableJar(project(":analysis:analysis-api-standalone:analysis-api-standalone-base",))
-    embeddableJar(project(":analysis:analysis-api-standalone:analysis-api-fir-standalone-base",))
+    embeddableJar(project(":analysis:analysis-api-standalone:analysis-api-standalone-base"))
+    embeddableJar(project(":analysis:analysis-api-standalone:analysis-api-fir-standalone-base"))
     embeddableJar(project(":analysis:analysis-api-standalone"))
     embeddableJar(project(":prepare:ide-plugin-dependencies:android-shims"))
     embeddableJar(commonDependency("javax.management:jmx"))
@@ -77,6 +77,8 @@ tasks.register<ShadowJar>("embeddable") {
         // we want to replace this class with our implementation, keep both out of the kotlin namespace
         exclude("com.intellij.util.containers.Unsafe")
         exclude("com.intellij.util.containers.AndroidUnsafe")
+
+        exclude("com.intellij.util.DebugAttachDetectorArgs")
     }
 
     // replace with our Android-specific implementation
@@ -88,6 +90,9 @@ tasks.register<ShadowJar>("embeddable") {
     exclude("javax/swing/SwingUtilities.class")
     relocate("javax.swing.SwingUtilities", "com.android.javax.swing.SwingUtilities")
 
+    // replace with our Android-compatible implementation
+    relocate("com.intellij.util.DebugAttachDetectorArgs", "com.android.com.intellij.util.DebugAttachDetectorArgs")
+
     packagesToRelocate.forEach {
         relocate(it, "$kotlinEmbeddableRootPackage.$it")
     }
@@ -98,7 +103,7 @@ tasks.register<ShadowJar>("embeddable") {
         exclude("org.fusesource.jansi.internal.CLibrary")
     }
 
-    exclude("org/checkerframework/**",)
+    exclude("org/checkerframework/**")
     exclude("org/intellij/lang/annotations/**")
     exclude("com/sun/jna/**")
     exclude("org/jetbrains/annotations/**")
diff --git a/prepare/ide-plugin-dependencies/android-shims/src/main/java/com/android/com/intellij/util/DebugAttachDetectorArgs.java b/prepare/ide-plugin-dependencies/android-shims/src/main/java/com/android/com/intellij/util/DebugAttachDetectorArgs.java
new file mode 100644
index 000000000000..16dbed150a90
--- /dev/null
+++ b/prepare/ide-plugin-dependencies/android-shims/src/main/java/com/android/com/intellij/util/DebugAttachDetectorArgs.java
@@ -0,0 +1,57 @@
+// Copyright 2000-2024 JetBrains s.r.o. and contributors. Use of this source code is governed by the Apache 2.0 license.
+package com.android.com.intellij.util;
+
+import com.intellij.openapi.diagnostic.Logger;
+import org.jetbrains.annotations.NotNull;
+import org.jetbrains.annotations.Nullable;
+
+import java.lang.management.ManagementFactory;
+import java.lang.reflect.InvocationTargetException;
+import java.util.Properties;
+
+/**
+ * Finds the debugger arguments passed to the IDE at startup.
+ */
+public final class DebugAttachDetectorArgs {
+  private static final Logger LOG = Logger.getInstance(DebugAttachDetectorArgs.class);
+  private static final @Nullable String DEBUG_ARGS = null;
+  private static final @Nullable Properties AGENT_PROPERTIES = null;
+
+  public static boolean isDebugEnabled() {
+    return DEBUG_ARGS != null;
+  }
+
+  public static boolean isAttached() {
+    if (!isDebugEnabled()) return false;
+    if (!isDebugServer()) return true;
+
+    Properties properties = AGENT_PROPERTIES;
+
+    // for now, return true if you cannot detect
+    if (properties == null) return true;
+
+    return isAttached(properties);
+  }
+
+  public static boolean canDetectAttach() {
+    return AGENT_PROPERTIES != null;
+  }
+
+  private static boolean isAttached(@NotNull Properties properties) {
+    String property = properties.getProperty("sun.jdwp.listenerAddress");
+    return property != null && property.isEmpty();
+  }
+
+  private static boolean isDebugServer() {
+    String args = DEBUG_ARGS;
+    return args != null && args.contains("server=y");
+  }
+
+  private static @Nullable String findDebugArgs() {
+    return null;
+  }
+
+  private static @Nullable Properties findAgentProperties() {
+    return null;
+  }
+}
-- 
2.53.0

